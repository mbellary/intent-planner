[project]
name = "intent-planner"
version = "0.1.0"
description = "Intent Planning Engine for Deterministic AI Governance Platform"
requires-python = ">=3.10"

dependencies = [
    "fastapi",
    "pydantic",
    "uvicorn",
    "pyyaml",
    "networkx",
    "httpx"
]

[tool.black]
line-length = 88
